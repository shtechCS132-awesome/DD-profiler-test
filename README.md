# DD-profiler-test

A minimal Rust binary crate. The starter `main` prints a simple greeting so you can verify your toolchain.

## Prerequisites
- Rust toolchain (recommended via [rustup](https://rustup.rs))

## Run locally
```bash
cargo run
```

## Build release binary
```bash
cargo build --release
```

## Run tests
```bash
cargo test
```
